use_nvm() {
  local node_version=$1

  nvm_sh=~/.nvm/nvm.sh
  if [[ -e $nvm_sh ]]; then
    source $nvm_sh
    nvm use $node_version
  fi
}

# Auto-activate .venv if it exists
layout_uv() {
    if [[ -d .venv ]]; then
        source_env .venv/bin/activate
    fi
}
